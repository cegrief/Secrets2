{
  "name": "Secrets",
  "private": true,
  "version": "0.0.0",
  "description": "Secrets Angular version",
  "repository": "https://github.com/cegrief/Secrets2.0",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000"
  },
  "dependencies": {
    "mongoose": "^3.8.17",
    "nodemailer": "^1.3.0"
  }
}
